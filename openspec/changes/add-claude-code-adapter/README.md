# add-claude-code-adapter

Secondary harness: Claude Code plugin build, hooks-based logger writing the same raw schema, correction capture, and a `claude -p` eval backend so the comparison matrix gains a harness axis.
