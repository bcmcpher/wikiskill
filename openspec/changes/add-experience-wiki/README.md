# add-experience-wiki

Maintainer agent and `/wikiskill-review` distil sampled raw sessions — failures and corrections first — into a persistent, git-versioned wiki of patterns scoped to component, model and harness.
